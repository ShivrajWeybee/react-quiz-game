import React from 'react'

export const Question = () => {
    return (
        <>
            {/* <div className='question-container'>
                <h3>{question[ques].questionStatement}</h3>
                <ul>
                    {
                        question[ques].answerStack.map(i => {
                            return (
                                <li key={i.option}>
                                    <input type='button' value={i.option} onClick={checkAnswer} />
                                </li>)
                        })
                    }
                </ul>
            </div>
            <p>Question - {ques + 1}/{question.length}</p> */}
        </>
    )
}